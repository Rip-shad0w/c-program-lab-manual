#include <stdio.h>
int main() {
    int num;
    int count = 0;
    while (scanf("%d", &num) == 1) {
        if (num % 2 != 0) {
            count++;
        }
        if (getchar() == '\n') break; 
    }
    printf("%d", count);
    return 0;
}
